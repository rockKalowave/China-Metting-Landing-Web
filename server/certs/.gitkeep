# 微信支付证书目录
#
# 请将以下文件放入此目录：
#
# 1. apiclient_key.pem    商户API证书私钥（必填，用于请求签名 + JSAPI调起支付签名）
# 2. apiclient_cert.pem   商户API证书公钥（可选，用于回调验签）
# 3. wechatpay_cert.pem   微信支付平台公钥（必填，用于验证微信回调签名）
#
# 证书获取方式：
# - apiclient_key.pem / apiclient_cert.pem：
#   登录 [微信商户平台](https://pay.weixin.qq.com) -> 账户中心 -> API安全 -> 申请API证书
#
# - wechatpay_cert.pem：
#   登录 [微信商户平台](https://pay.weixin.qq.com) -> 账户中心 -> API安全 -> 获取平台证书
#   或调用 GET https://api.mch.weixin.qq.com/v3/certificates 接口下载
